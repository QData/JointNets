### simule, jeek shared graph same as shared edge between individual graphs?
### jeek cannot run parallel


# function containing code to test
test <- function(X){
  {
    data(cancer)
    data("exampleData")
    result = jeek(X = exampleData, 0.3, covType = "cov", parallel = FALSE)
    plot(result)
  }
}
